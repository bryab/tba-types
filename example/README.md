This is starter template for writing scripts for Toon Boom applications in Typescript.

If you run the npm script `build` like so:

`npm run build`

You should see a `.js` file appear in the `./dist` directory.